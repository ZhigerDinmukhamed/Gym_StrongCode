# Тесты Gym-StrongCode

## Структура тестов

### Unit-тесты (`tests/unit/`)
Изолированные тесты компонентов с использованием моков:

- **`user_repository_test.go`** - тесты репозитория пользователей
- **`booking_repository_test.go`** - тесты репозитория бронирований
- **`repository_test.go`** - общие тесты репозиториев (залы, подписки)
- **`auth_service_test.go`** - тесты сервиса аутентификации с моками
- **`service_test.go`** - тесты других сервисов

### Интеграционные тесты (`tests/integration/`)
Тесты полного API с реальной БД:

- **`api_test.go`** - основные API эндпоинты
- **`auth_test.go`** - тесты аутентификации и авторизации
- **`user_test.go`** - сценарии работы с пользователями
- **`edge_cases_test.go`** - граничные случаи, безопасность, валидация

### Утилиты (`tests/testutils/`)
Вспомогательные функции для тестов:

- **`database.go`** - создание in-memory БД для тестов
- **`fixtures.go`** - создание тестовых данных

## Запуск тестов

### Все тесты
```bash
go test ./tests/...
```

### Только unit-тесты
```bash
go test ./tests/unit/...
```

### Только интеграционные тесты
```bash
go test ./tests/integration/...
```

### С подробным выводом
```bash
go test -v ./tests/...
```

### С покрытием кода
```bash
go test -cover ./tests/...
go test -coverprofile=coverage.out ./tests/...
go tool cover -html=coverage.out
```

### Конкретный тест
```bash
go test -v -run TestUserRepository_Create ./tests/unit/...
```

### С race detector
```bash
go test -race ./tests/...
```

## Покрытие тестами

### Unit-тесты проверяют:
- ✅ CRUD операции в репозиториях
- ✅ Бизнес-логику в сервисах
- ✅ Работу с базой данных
- ✅ Обработку ошибок
- ✅ Валидацию данных

### Интеграционные тесты проверяют:
- ✅ HTTP эндпоинты
- ✅ Аутентификацию и авторизацию
- ✅ JWT токены
- ✅ Роли пользователей (admin/user)
- ✅ Полные сценарии (регистрация → логин → действия)
- ✅ Публичные и защищенные эндпоинты
- ✅ Валидацию входных данных
- ✅ Обработку ошибок (дубликаты, неверные данные)
- ✅ Безопасность (SQL-инъекции, невалидные токены)
- ✅ Граничные случаи (большие payload, конкурентность)

## Что покрывают тесты

### Репозитории
- User: Create, GetByEmail, GetByID, List, Delete
- Booking: Create, Exists, GetByUser, Cancel
- Gym: Create, List, Update, Delete
- Membership: CRUD операции

### Сервисы
- AuthService: Register, Login (успех и ошибки)
- GymService: Create, List
- Другие сервисы с использованием моков

### API эндпоинты
- POST /api/users/register
- POST /api/users/login
- GET /api/me (требует auth)
- GET /api/health
- Публичные: /api/gyms, /api/classes, /api/memberships, /api/trainers
- Админские: /admin/* (требуют admin роль)

### Безопасность
- ✅ SQL-инъекции
- ✅ Невалидные токены
- ✅ Неавторизованный доступ
- ✅ Доступ не-админов к админским эндпоинтам
- ✅ Дубликаты email при регистрации
- ✅ Неверные пароли

## Лучшие практики

### ✅ Что сделано правильно:
1. **Изоляция тестов** - каждый тест использует свою in-memory БД
2. **Cleanup** - автоматическая очистка ресурсов через `t.Cleanup()`
3. **Моки** - использование testify/mock для unit-тестов
4. **Фикстуры** - переиспользуемые хелперы для создания тестовых данных
5. **Table-driven tests** - для проверки множества сценариев
6. **Понятные имена** - `TestUserRepository_Create`, `TestLogin_InvalidCredentials`
7. **Проверка ошибок** - require.NoError() для критичных операций, assert.Error() для ожидаемых
8. **Независимость** - тесты можно запускать в любом порядке

### 🔧 Что можно улучшить:
1. Добавить бенчмарки для производительности
2. Расширить покрытие edge cases
3. Добавить тесты для кэша Redis (если используется)
4. Добавить E2E тесты с реальным сервером
5. Тесты для worker'ов и фоновых задач
6. Тесты для middleware (rate limiting)
7. Нагрузочное тестирование

## Пример добавления нового теста

```go
func TestMyNewFeature(t *testing.T) {
    // Setup
    db := testutils.SetupTestDB(t)
    repo := repository.NewMyRepository(db)
    
    // Execute
    result, err := repo.DoSomething()
    
    // Assert
    require.NoError(t, err)
    assert.NotNil(t, result)
    assert.Equal(t, expected, result.Field)
}
```

## CI/CD интеграция

Добавьте в `.github/workflows/test.yml`:

```yaml
name: Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v4
        with:
          go-version: '1.23'
      - run: go test -v -race -coverprofile=coverage.out ./tests/...
      - run: go tool cover -func=coverage.out
```
